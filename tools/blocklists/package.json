{
  "name": "@safe-browse/blocklists",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "compile": "tsx src/cli.ts"
  },
  "dependencies": { "@safe-browse/contracts": "0.1.0" },
  "devDependencies": { "@types/node": "^24.0.0", "tsx": "^4.20.0", "typescript": "^5.9.0", "vitest": "^4.0.0" }
}
