{
  "name": "@safe-browse/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "webextension-polyfill": "^0.12.0" },
  "devDependencies": { "@types/webextension-polyfill": "^0.12.0", "esbuild": "^0.25.0", "typescript": "^5.9.0", "vitest": "^4.0.0" }
}
